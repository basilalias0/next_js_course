import { Metadata } from "next"

export const metadata:Metadata = {
    title:"Profile",
    description:"Made by Basil"
}

export default function profile() {
    return(
        <>
        <h1>Profile</h1>
        </>
    )
}